Schema allows you to encode and decode tables into binary. This is useful for when you want to ping a table but don't want to waste bytes doing so. This is 100% an advanced library so reading everything is highly recommended.

Oh also this script is wip